<% @title = "Edição de dados, #{@company.name}" %>
<% provide(:title, @title) %>
<div class='row'>
  <div class='page-header'>
    <h1><%= @title %></h1>
  </div>
  <div class='span8'>
    <%= render 'form' %>
  </div>
</div>